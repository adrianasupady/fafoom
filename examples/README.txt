Usage:

python ga.py parameter_file_name

If you want to restart your calculation (at least one genetic algorithm iteration must be completed), use:

python ga.py parameter_file_name

Provided example allows for optimization of alanine dipeptide.
